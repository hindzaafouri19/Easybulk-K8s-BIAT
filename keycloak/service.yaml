apiVersion: v1
kind: Service
metadata:
  name: keycloak-auth
  namespace: esb
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: keycloak-auth
