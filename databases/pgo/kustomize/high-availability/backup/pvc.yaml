apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-restore-pvc
  namespace: esb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs-client

