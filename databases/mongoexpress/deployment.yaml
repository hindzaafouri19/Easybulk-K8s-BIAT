apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-express
  namespace: esb
  labels:
    app: mongo-express
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-express
  template:
    metadata:
      labels:
        app: mongo-express
    spec:
      containers:
        - name: mongo-express
          image: mongo-express
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: mongo-express-config
          ports:
            - containerPort: 8081

